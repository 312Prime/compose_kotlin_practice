<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeTest"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ComposeTest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".practice.BaseToolPracticeActivity"
            android:exported="true" />
        <activity
            android:name=".practice.RetrofitPracticeActivity"
            android:exported="true" />
        <activity
            android:name=".practice.DrawerPracticeActivity"
            android:exported="true" />
        <activity
            android:name=".practice.DialogPracticeActivity"
            android:exported="true" />
        <activity
            android:name=".practice.CanvasPracticeActivity"
            android:exported="true" />
        <activity
            android:name=".function.ResumeActivity"
            android:exported="true" />
        <activity
            android:name=".function.DiceActivity"
            android:exported="true" />
    </application>

</manifest>